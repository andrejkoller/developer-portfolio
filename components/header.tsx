export const Header = () => {
  return (
    <header
      className="layout-container flex items-center justify-between h-(--header-height) fade-in"
      aria-label="Site header"
    >
      <div className="flex flex-col items-start">
        {/* SVG Logo coming soon */}
      </div>
    </header>
  );
};
