export const Header = () => {
  return (
    <header
      className="flex items-center justify-between h-(--header-height) w-full max-w-2xl mx-auto px-4 fade-in"
      aria-label="Site header"
    >
      <div className="flex flex-col items-start">
        {/* SVG Logo coming soon */}
      </div>
    </header>
  );
};
